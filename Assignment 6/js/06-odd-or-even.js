// Initializing the variables
var i;

// Looping over the numbers to print odd/even
for (i = 0; i <= 15; i += 1) {
    if (i % 2 === 0) {
        window.console.log(i + " is even");
    }
    else {
        window.console.log(i + " is odd");
    }
}